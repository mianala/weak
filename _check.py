import sys; print('python', sys.version, flush=True)
print('--imports--', flush=True)
import numpy; print('numpy', numpy.__version__, flush=True)
import torch; print('torch', torch.__version__, 'cuda', torch.cuda.is_available(), flush=True)
import transformers; print('transformers', transformers.__version__, flush=True)
import datasets; print('datasets', datasets.__version__, flush=True)
import soundfile; print('soundfile', soundfile.__version__, flush=True)
import librosa; print('librosa', librosa.__version__, flush=True)
import evaluate; print('evaluate', evaluate.__version__, flush=True)
print('all ok', flush=True)
